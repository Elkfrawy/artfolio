
<div class="container">

  <div class="row justify-content-center">
    <div class="col-md-6">
    <div class="card">
      <header class="card-header">
        <h1 class="card-title mt-2 display-4">Create Your Artwork</h1>
      </header>
      <article class="card-body">

        <form class ="create-new-form" id = "createForm" method ="POST" enctype="multipart/form-data">
          <div class="form-group">
            <label for="title">Title</label>
            <input type="text" class="form-control" id="title" name = "title" value="{{artwork.title}}" required>
            <small id="title-help" class="form-text text-muted">Choose a title that ties all your images together</small>
          </div>
          <div class="form-group">
            <label for="Description">Description</label>
            <textarea class="form-control" id="Description" name = "description" rows="3">{{artwork.description}}</textarea>
          </div>
          <div class="form-group">
            <label for="createDate">Creation Date</label>
            <input type="date" class="form-control" id="createDate" name = "createDate" value="{{artwork.createDate}}">
            <small id="date-help" class="form-text text-muted">Pick the date when your image(s) was created</small>
          </div>
          <div class="form-group">
            <label for="category">Category</label>
            <input type="text" class="form-control" id="category" name = "category" value="{{artwork.category}}">
          </div>
          <div class="form-group">
            <label for="image">Upload single/multiple Images</label>
            <input type="file" class="form-control-file" name="image" id="image" accept="image/*" multiple/>
          </div>
          <div class="form-group">
            <button type="submit" class="btn btn-success">Submit</button>
          </div>
          {{#if errors}}
            <ul class="text-danger">
            {{#each errors}}
            <li>
            {{this}}   
            </li>
            {{/each}}
          </ul>
        {{/if}} 
        </form>

      </article>
    </div>
    </div>
  </div> 
</div>