<div class="col-xs-4 gallery-item">
  <a href="/artworks/{{_id}}">
  <div class="album">
      {{#each pictures}}
    <img src="{{pictureUrl}}" alt="" />
      {{/each}}
  </div>
  <p class="h6">{{title}}</p>
  </a>
</div>